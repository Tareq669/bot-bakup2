# 💾 نظام النسخ الاحتياطي - Backup System

## نظرة عامة | Overview

نظام نسخ احتياطي شامل ومتقدم للبوت يدعم:
- النسخ الاحتياطي الكامل لجميع البيانات
- النسخ الاحتياطي التدريجي (فقط التغييرات)
- ضغط الملفات لتوفير المساحة
- الاستعادة الآمنة من النسخ الاحتياطية
- جدولة تلقائية للنسخ الاحتياطي
- إدارة متقدمة للنسخ القديمة

A comprehensive and advanced backup system for the bot that supports:
- Full backup of all data
- Incremental backups (only changes)
- File compression to save space
- Safe restoration from backups
- Automatic backup scheduling
- Advanced management of old backups

---

## 📦 أنواع النسخ الاحتياطية | Backup Types

### 1. النسخ الكامل | Full Backup
**الوصف:** نسخة احتياطية شاملة لجميع المجموعات في قاعدة البيانات

**المجموعات المشمولة:**
- 👥 Users - المستخدمين
- 👥 Groups - المجموعات
- 💰 Transactions - المعاملات المالية
- 🎮 GameStats - إحصائيات الألعاب
- 📝 Content - المحتوى
- ⚙️ Config - الإعدادات
- 🏆 Teams - الفرق
- 📅 Events - الأحداث
- 🎯 Auctions - المزادات

**المميزات:**
- ضغط تلقائي (gzip)
- معلومات إحصائية شاملة
- تاريخ ووقت النسخة
- حجم الملف المضغوط

**الاستخدام:**
```
/backup ثم اختر "📦 نسخ كامل"
```

---

### 2. نسخ المستخدمين | Users Backup
**الوصف:** نسخة احتياطية سريعة لبيانات المستخدمين فقط

**الاستخدام:**
```
/backup ثم اختر "👥 نسخ المستخدمين"
```

**الاستخدامات المثالية:**
- نسخ سريع قبل التحديثات
- مراقبة نمو قاعدة المستخدمين
- استرجاع سريع لبيانات المستخدمين

---

### 3. النسخ التدريجي | Incremental Backup
**الوصف:** نسخة احتياطية للتغييرات فقط منذ آخر نسخة كاملة

**المميزات:**
- أسرع من النسخ الكامل
- يستهلك مساحة أقل
- يحفظ فقط البيانات المتغيرة
- مرتبط بآخر نسخة كاملة

**الاستخدام:**
```
/backup ثم اختر "♻️ نسخ تدريجي"
```

---

## ⚙️ الإعدادات التلقائية | Automatic Settings

### الجدولة التلقائية | Automatic Scheduling

#### 1. نسخة احتياطية كاملة يومية
**التوقيت:** 2:00 صباحاً (كل يوم)
**النوع:** نسخة كاملة مضغوطة
**الهدف:** حماية شاملة للبيانات

#### 2. نسخة احتياطية سريعة للمستخدمين
**التوقيت:** كل 6 ساعات
**النوع:** نسخة المستخدمين فقط
**الهدف:** حماية متكررة لبيانات المستخدمين

#### 3. تنظيف النسخ القديمة
**التوقيت:** أسبوعياً (الأحد 3:00 صباحاً)
**الإجراء:** حذف النسخ الأقدم من 30 يوماً
**الهدف:** توفير المساحة

---

## 🔧 الاستخدام | Usage

### للمالكين فقط | For Owners Only

#### الأوامر الأساسية | Basic Commands

```bash
/backup              # قائمة خيارات النسخ الاحتياطي
```

#### خيارات القائمة | Menu Options

1. **📦 نسخ كامل** - Full backup of all data
2. **👥 نسخ المستخدمين** - Users data only
3. **📋 قائمة النسخ** - List available backups
4. **📊 الإحصائيات** - Backup statistics
5. **♻️ نسخ تدريجي** - Incremental backup

---

## 📊 الإحصائيات | Statistics

عند اختيار "📊 الإحصائيات" ستحصل على:

```
📊 إحصائيات النسخ الاحتياطية

📈 عدد النسخ: 15
🔵 نسخ كاملة: 7
🗜️ نسخ مضغوطة: 12
💾 الحجم الإجمالي: 45.3 MB

📅 أحدث نسخة: 2024-02-16 14:30
📅 أقدم نسخة: 2024-01-15 02:00
```

---

## 📋 قائمة النسخ | Backup List

عند اختيار "📋 قائمة النسخ" ستحصل على:

```
📋 النسخ الاحتياطية المتاحة

1. 🔵 📦 full_backup_1708092000000.json.gz
   📊 12.5 MB | كاملة
   📅 2024-02-16 14:00:00

2. 🟢 📄 users_backup_2024-02-16T08-30-00.json
   📊 3.2 MB | مستخدمين
   📅 2024-02-16 08:30:00

... وعدد 8 نسخة أخرى

📈 المجموع: 10 نسخة احتياطية
```

**الرموز:**
- 🔵 = نسخة كاملة
- 🟢 = نسخة جزئية
- 📦 = ملف مضغوط
- 📄 = ملف عادي

---

## 🔐 الأمان | Security

### الاحتياطات الأمنية | Security Precautions

1. **الصلاحيات:** فقط مالكي البوت يمكنهم الوصول لنظام النسخ
2. **الموقع:** النسخ تُحفظ في مجلد `backups/` المحمي
3. **Git:** المجلد مستثنى من Git (`.gitignore`)
4. **الاستعادة:** تتطلب تأكيد إضافي لحماية البيانات الحالية

### معلومات حساسة | Sensitive Data

⚠️ **تحذير:** النسخ الاحتياطية تحتوي على:
- بيانات المستخدمين الشخصية
- الأرصدة المالية
- معلومات المجموعات
- إعدادات النظام

**احرص على:**
- عدم مشاركة ملفات النسخ الاحتياطي
- تخزين النسخ في مكان آمن
- استخدام كلمة مرور قوية للسيرفر

---

## 🔄 الاستعادة | Restoration

### الاستعادة من نسخة احتياطية | Restore from Backup

**حالياً:** النظام يدعم قراءة النسخ الاحتياطية ومعاينتها

**قريباً:** سيتم إضافة:
- استعادة كاملة للبيانات
- استعادة انتقائية (مجموعات محددة)
- خيارات الدمج (استبدال أو تخطي)
- نسخة احتياطية تلقائية قبل الاستعادة

### خطوات الاستعادة المستقبلية:

1. اختيار النسخة الاحتياطية
2. معاينة محتوى النسخة
3. اختيار المجموعات للاستعادة
4. تأكيد العملية
5. إنشاء نسخة احتياطية تلقائية قبل الاستعادة
6. تنفيذ الاستعادة
7. التحقق من النتائج

---

## 📁 هيكل الملفات | File Structure

### تنسيق النسخة الكاملة | Full Backup Format

```json
{
  "timestamp": "2024-02-16T14:00:00.000Z",
  "version": "2.0",
  "metadata": {
    "botVersion": "1.0.0",
    "dataVersion": "2.0",
    "createdAt": "2024-02-16T14:00:00.000Z",
    "compressed": true
  },
  "collections": {
    "users": {
      "count": 1500,
      "data": [...]
    },
    "groups": {
      "count": 25,
      "data": [...]
    },
    "transactions": {
      "count": 5000,
      "data": [...]
    }
    // ... المزيد من المجموعات
  },
  "statistics": {
    "totalUsers": 1500,
    "totalGroups": 25,
    "totalTransactions": 5000,
    "totalGameStats": 3000
  }
}
```

---

## 🛠️ استخدام برمجي | Programmatic Usage

### استخدام النظام من الكود | Using from Code

```javascript
const BackupSystem = require('./utils/backupSystem');
const backup = new BackupSystem();

// نسخة احتياطية كاملة
const result = await backup.fullBackup(true); // with compression
console.log(result);

// نسخة احتياطية للمستخدمين
const usersResult = await backup.backupUsers();
console.log(usersResult);

// قائمة النسخ الاحتياطية
const backups = backup.listBackups();
console.log(backups);

// إحصائيات
const stats = backup.getBackupStats();
console.log(stats);

// نسخة تدريجية
const incrementalResult = await backup.incrementalBackup();
console.log(incrementalResult);
```

---

## 🐛 استكشاف الأخطاء | Troubleshooting

### المشاكل الشائعة | Common Issues

#### 1. فشل النسخ الاحتياطي
**الحل:**
- تحقق من اتصال قاعدة البيانات
- تحقق من المساحة المتاحة
- راجع سجلات الأخطاء

#### 2. النسخ الاحتياطية كبيرة جداً
**الحل:**
- استخدم الضغط (تلقائي في النسخ الكامل)
- احذف النسخ القديمة
- استخدم النسخ التدريجي

#### 3. عدم ظهور النسخ في القائمة
**الحل:**
- تحقق من وجود مجلد `backups/`
- تحقق من صلاحيات القراءة
- تأكد من صيغة الملفات (.json أو .json.gz)

---

## 📈 أفضل الممارسات | Best Practices

### توصيات الاستخدام | Usage Recommendations

1. **نسخ احتياطية منتظمة:**
   - اعتمد على الجدولة التلقائية
   - نسخة يدوية قبل التحديثات الكبرى

2. **إدارة المساحة:**
   - احذف النسخ القديمة بانتظام
   - استخدم الضغط دائماً للنسخ الكاملة

3. **الأمان:**
   - لا تشارك ملفات النسخ الاحتياطي
   - احفظ نسخة خارج السيرفر
   - استخدم تشفير للملفات الحساسة

4. **الاختبار:**
   - اختبر الاستعادة بشكل دوري
   - تحقق من صحة النسخ الاحتياطية

5. **المراقبة:**
   - راجع الإحصائيات بانتظام
   - تحقق من نجاح النسخ التلقائية
   - راقب حجم المجلد

---

## 🔮 التطويرات المستقبلية | Future Enhancements

### قريباً | Coming Soon

- [ ] استعادة كاملة وآمنة
- [ ] نسخ احتياطية سحابية (Google Drive, Dropbox)
- [ ] تشفير الملفات
- [ ] إشعارات عند فشل النسخ
- [ ] واجهة ويب لإدارة النسخ
- [ ] مقارنة بين نسختين
- [ ] استعادة انتقائية لمستخدم محدد

---

## 📞 الدعم | Support

في حال واجهت مشاكل:

1. راجع سجلات البوت: `logs/`
2. تحقق من رسائل الخطأ
3. تواصل مع المطورين:
   - @JOAmeer
   - @llTDTll

---

## 📝 ملاحظات | Notes

- النظام يحفظ النسخ في `backups/` بشكل افتراضي
- الضغط يوفر حتى 70% من المساحة
- النسخ التلقائية تعمل فقط عند تشغيل البوت
- يُنصح بالاحتفاظ بنسخة واحدة على الأقل خارج السيرفر

---

## 📜 الترخيص | License

هذا النظام جزء من البوت ويخضع لنفس الترخيص.

---

**آخر تحديث:** 2024-02-16
**الإصدار:** 2.0
**المطور:** TareqFX

# 🤝 دليل المساهمة - Contributing Guide

شكراً لاهتمامك بالمساهمة في Arab Telegram Bot! نحن نرحب بجميع أنواع المساهمات.

## 📋 جدول المحتويات

- [كيفية المساهمة](#كيفية-المساهمة)
- [الإعداد للتطوير](#الإعداد-للتطوير)
- [معايير الكود](#معايير-الكود)
- [عملية Pull Request](#عملية-pull-request)
- [الإبلاغ عن الأخطاء](#الإبلاغ-عن-الأخطاء)
- [اقتراح ميزات جديدة](#اقتراح-ميزات-جديدة)

## 🚀 كيفية المساهمة

### أنواع المساهمات المرحب بها

- 🐛 إصلاح الأخطاء (Bug fixes)
- ✨ ميزات جديدة (New features)
- 📝 تحسين التوثيق (Documentation improvements)
- 🎨 تحسينات UI/UX
- 🧪 إضافة اختبارات (Adding tests)
- ⚡ تحسينات الأداء (Performance improvements)
- 🔒 تحسينات الأمان (Security improvements)

## 🛠️ الإعداد للتطوير

### 1. Fork المشروع

انقر على زر "Fork" في أعلى الصفحة لإنشاء نسخة من المشروع في حسابك.

### 2. استنساخ المشروع

```bash
git clone https://github.com/YOUR_USERNAME/bot.git
cd bot
```

### 3. إضافة المستودع الأصلي كـ upstream

```bash
git remote add upstream https://github.com/Tareq669/bot.git
```

### 4. تثبيت المكتبات

```bash
npm install
```

### 5. إعداد البيئة

```bash
cp .env.example .env
# قم بتعديل .env وإضافة البيانات المطلوبة
```

### 6. تشغيل البوت في وضع التطوير

```bash
npm run dev
```

## 📝 معايير الكود

### JavaScript Style Guide

نستخدم ESLint و Prettier للحفاظ على جودة الكود:

```bash
# فحص الكود
npm run lint

# إصلاح المشاكل تلقائياً
npm run lint:fix

# تنسيق الكود
npm run format
```

### قواعد الكتابة

#### 1. استخدم const و let (ليس var)
```javascript
// ✅ صحيح
const userId = ctx.from.id;
let counter = 0;

// ❌ خطأ
var userId = ctx.from.id;
```

#### 2. استخدم async/await بدلاً من callbacks
```javascript
// ✅ صحيح
async function getUser(userId) {
  const user = await User.findOne({ userId });
  return user;
}

// ❌ خطأ
function getUser(userId, callback) {
  User.findOne({ userId }, (err, user) => {
    callback(err, user);
  });
}
```

#### 3. معالجة الأخطاء دائماً
```javascript
// ✅ صحيح
async function handleCommand(ctx) {
  try {
    const result = await someOperation();
    logger.success('Operation completed');
  } catch (error) {
    logger.error('Operation failed:', error);
    ctx.reply(ERROR_MESSAGES.GENERIC);
  }
}
```

#### 4. استخدم الثوابت من constants.js
```javascript
// ✅ صحيح
const { ERROR_MESSAGES, ECONOMY } = require('../config/constants');
ctx.reply(ERROR_MESSAGES.NO_PERMISSION);

// ❌ خطأ
ctx.reply('❌ ليس لديك صلاحية');
```

#### 5. استخدم Logger بدلاً من console
```javascript
// ✅ صحيح
const { logger } = require('../utils/logger');
logger.info('User logged in', { userId });
logger.error('Failed to save', error);

// ❌ خطأ
console.log('User logged in', userId);
console.error('Failed to save', error);
```

### تسمية الملفات والمتغيرات

- **Handlers**: `camelCase` - `adminHandlers.js`, `gameHandlers.js`
- **Classes**: `PascalCase` - `AdminHandlers`, `GameManager`
- **Functions**: `camelCase` - `handleCommand()`, `getUserData()`
- **Constants**: `UPPER_SNAKE_CASE` - `ERROR_MESSAGES`, `ECONOMY`
- **Variables**: `camelCase` - `userId`, `userProfile`

### JSDoc Comments

استخدم JSDoc لتوثيق الوظائف:

```javascript
/**
 * Get user profile from database
 * @param {number} userId - Telegram user ID
 * @returns {Promise<Object>} User profile object
 */
async function getUserProfile(userId) {
  const user = await User.findOne({ userId });
  return user;
}
```

## 🧪 الاختبارات

### كتابة الاختبارات

كل ميزة جديدة يجب أن تحتوي على اختبارات:

```javascript
// test/unit/myFeature.test.js
describe('MyFeature', () => {
  it('should do something', () => {
    expect(result).toBe(expected);
  });

  it('should handle errors', async () => {
    await expect(asyncFunction()).rejects.toThrow();
  });
});
```

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
npm test

# تشغيل مع المراقبة
npm run test:watch

# تشغيل مع Coverage
npm run test:coverage
```

## 🔄 عملية Pull Request

### 1. إنشاء فرع جديد

```bash
# للميزات الجديدة
git checkout -b feature/amazing-feature

# لإصلاح الأخطاء
git checkout -b fix/bug-description

# للتوثيق
git checkout -b docs/documentation-update
```

### 2. تنفيذ التغييرات

- اتبع معايير الكود المذكورة أعلاه
- أضف اختبارات للميزات الجديدة
- حدّث التوثيق إذا لزم الأمر

### 3. Commit التغييرات

استخدم رسائل commit واضحة:

```bash
# ✅ جيد
git commit -m "Add user authentication system"
git commit -m "Fix memory leak in game handler"
git commit -m "Update README with new features"

# ❌ غير واضح
git commit -m "Update"
git commit -m "Fix bug"
git commit -m "Changes"
```

### 4. Push إلى Fork الخاص بك

```bash
git push origin feature/amazing-feature
```

### 5. فتح Pull Request

1. اذهب إلى صفحة GitHub الخاصة بـ fork
2. انقر "Compare & pull request"
3. املأ النموذج:
   - **العنوان**: وصف موجز للتغييرات
   - **الوصف**: شرح مفصل للتغييرات
   - **المشاكل المرتبطة**: #issue_number
   - **نوع التغيير**: ميزة/إصلاح/توثيق
   - **Checklist**: تأكد من إكمال جميع النقاط

### نموذج Pull Request

```markdown
## وصف التغييرات
وصف واضح ومختصر للتغييرات

## نوع التغيير
- [ ] ميزة جديدة
- [ ] إصلاح خطأ
- [ ] تحسين أداء
- [ ] تحديث توثيق

## الاختبار
كيف تم اختبار التغييرات؟

## Checklist
- [ ] الكود يتبع معايير المشروع
- [ ] تم إضافة اختبارات
- [ ] جميع الاختبارات تمر
- [ ] تم تحديث التوثيق
- [ ] لا توجد تحذيرات من ESLint
```

## 🐛 الإبلاغ عن الأخطاء

### قبل الإبلاغ

1. تأكد من أن المشكلة لم تُبلغ عنها مسبقاً
2. تحقق من أنك تستخدم آخر إصدار
3. حاول إعادة إنتاج المشكلة

### نموذج الإبلاغ

```markdown
## وصف المشكلة
وصف واضح ومختصر للمشكلة

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. انقر على '...'
3. انظر الخطأ

## السلوك المتوقع
ما الذي كان يجب أن يحدث

## السلوك الفعلي
ما حدث بالفعل

## لقطات شاشة
إضافة لقطات شاشة إن أمكن

## البيئة
- OS: [e.g. Ubuntu 22.04]
- Node.js: [e.g. 18.17.0]
- Bot Version: [e.g. 1.0.0]

## معلومات إضافية
أي معلومات أخرى مفيدة
```

## ✨ اقتراح ميزات جديدة

### قبل الاقتراح

1. تحقق من أن الميزة غير موجودة
2. تأكد من أنها تتماشى مع أهداف المشروع
3. فكر في الفائدة للمستخدمين

### نموذج الاقتراح

```markdown
## وصف الميزة
وصف واضح للميزة المقترحة

## المشكلة التي تحلها
لماذا هذه الميزة مفيدة؟

## الحل المقترح
كيف يجب أن تعمل الميزة؟

## البدائل المدروسة
هل فكرت في حلول أخرى؟

## معلومات إضافية
أي معلومات أو سياق إضافي
```

## 📚 الموارد المفيدة

### التوثيق
- [Telegraf Documentation](https://telegraf.js.org/)
- [MongoDB Documentation](https://docs.mongodb.com/)
- [Jest Documentation](https://jestjs.io/)

### دلائل المشروع
- [README.md](./README.md)
- [ARCHITECTURE.md](./ARCHITECTURE.md)
- [USER_GUIDE.md](./USER_GUIDE.md)

## 💬 التواصل

- **GitHub Issues**: للأخطاء والاقتراحات
- **Pull Requests**: للمساهمات بالكود
- **Discussions**: للأسئلة والنقاشات

## 📜 قواعد السلوك

### نتوقع من جميع المساهمين:

✅ **افعل:**
- كن محترماً ومهذباً
- قدّر وقت وجهد الآخرين
- قدم نقد بنّاء
- اقبل النقد البنّاء
- ركز على ما هو أفضل للمجتمع

❌ **لا تفعل:**
- استخدام لغة مسيئة
- الهجوم الشخصي
- التحرش بأي شكل
- نشر معلومات خاصة للآخرين
- السلوك غير المهني

## 🎉 شكراً لك!

مساهمتك تساعد في تحسين هذا المشروع. نقدر وقتك وجهدك! 

---

إذا كانت لديك أي أسئلة، لا تتردد في فتح issue أو التواصل مع المطورين.

صُنع بـ ❤️ من قِبل المجتمع

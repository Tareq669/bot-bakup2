# 🔧 دليل حل مشكلة الاتصال بـ Telegram

## المشكلة
البوت لا يستطيع الاتصال بـ `api.telegram.org` بسبب حجب الشبكة.

## ✅ الحلول السريعة (اختر واحد)

### 1️⃣ Cloudflare WARP (مجاني - موصى به)
أسرع حل وأسهل:

1. حمّل من: https://one.one.one.one/
2. ثبّت وشغّل WARP
3. شغّل البوت: `npm start`

✨ **يعمل تلقائياً بدون إعدادات إضافية!**

---

### 2️⃣ VPN تجاري (الأسرع)
- ProtonVPN (مجاني)
- Windscribe (10GB مجاناً)
- أي VPN تستخدمه عادةً

بعد تشغيل VPN:
```bash
npm start
```

---

### 3️⃣ Proxy محلي (للمحترفين)

#### أ) إذا عندك V2Ray/Clash/Shadowsocks:
افتح ملف `.env` وأضف:
```env
# للـ HTTP Proxy (مثلاً Clash)
HTTPS_PROXY=http://127.0.0.1:7890

# أو للـ SOCKS Proxy (مثلاً V2Ray)
SOCKS_PROXY=socks5://127.0.0.1:1080
```

#### ب) تثبيت Tor (SOCKS Proxy):
```powershell
# باستخدام Chocolatey
choco install tor

# أو حمّل من: https://www.torproject.org/download/
```

بعدها أضف في `.env`:
```env
SOCKS_PROXY=socks5://127.0.0.1:9050
```

---

### 4️⃣ تحقق من الجدار الناري
إذا كنت في شبكة شركة/جامعة، قد يكون Telegram محجوب:

```powershell
# اختبار الاتصال
Test-NetConnection api.telegram.org -Port 443
```

إذا فشل، ستحتاج VPN أو Proxy.

---

## 🚀 بعد أي حل من الحلول:
```bash
npm start
```

## ✅ التأكد من نجاح الاتصال
إذا ظهرت هذه الرسائل، البوت يعمل:
```
[INFO] ✅ تم تشغيل البوت بنجاح!
[INFO] 🎯 البوت مستعد و ينتظر الرسائل...
```

---

## 🆘 مازالت المشكلة موجودة؟
تواصل مع المطورين:
- @JOAmeer
- @llTDTll
